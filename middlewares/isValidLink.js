/**
 * @description - This middleware check if the link is valid or not.
 * It checks the expire at attribute and determine whether to proceed 
 * to genDownloadFile.js or not.
 */

// TODO
const isValidLink = async (req, res, next) => {

}

module.exports = isValidLink;